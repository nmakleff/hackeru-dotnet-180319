using System;

public class Program
{
	public void Main()
	{
        string S1 = Console.ReadLine();

        int Count = 0;
        for (int i = 0; i < S.Length; i++)
        {
            if (S[i] == 'A')
                Count++;
        }

        Console.WriteLine("A found in string " + Count + " times");
	}
}
